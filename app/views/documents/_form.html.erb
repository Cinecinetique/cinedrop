<%= form_for(@document,:html => {:multipart => true}) do |f| %>
  <% if @document.errors.any? %>
    <div class="alert alert-error" id="error_explanation">
      <%= pluralize(@document.errors.count, "error") %> prohibited this document from being saved:

      <ul>
      <% @document.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>

    <div class="field">
    <%= f.label "Scene number" %><br>
    <%= f.text_field :scene_number %>
  </div>

  <div class="field">
    <%= f.label :data %><br>
    <%= f.file_field :data %>
    <span class="label label-important">For this prototype, maximum file size is 40 MB</span>
  </div>
  <div class="actions">
    <%= f.hidden_field :project_id, :value => "#{session[:current_project]}" %>
    <%= f.submit %>
  </div>
<% end %>


