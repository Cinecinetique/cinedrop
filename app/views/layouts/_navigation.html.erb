<%= link_to "Cinecinetique", root_path, :class => 'brand' %>
<ul class="nav">
	<li>
    <%= link_to('Statistics', documents_path, :method=>'delete') %>
    </li>
	<li>
    <%= link_to('Documentation', documents_path, :method=>'delete') %>
    </li>
  </ul>
  <p class="navbar-text pull-right">
  <% if session[:user_id] %>
    logged in as <%= session[:email] %> (<%= link_to('Logout', logout_path, :method=>'delete') %>)
  <% else %>
    <%= link_to('Login', login_path)  %>
  <% end %>
  </p>
