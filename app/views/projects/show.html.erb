<p>
  <strong>Project Name:</strong>
  <%= @project.name %>
</p>

<%= link_to 'New Document', new_document_path(:project_id => @project.id), :class => "btn btn-primary" %>

<table class="table table-hover">
  <thead>
    <tr>
      <th>Scene number</th>
      <th>Thumbnail</th>
      <th>Name</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <%= render(@project.documents.order("updated_at DESC")) %>
  </tbody>
</table>

<%= link_to 'New Document', new_document_path(:project_id => @project.id), :class => "btn btn-primary" %>
 |
<%= link_to 'Back', projects_path, :class => "btn btn-link" %>
